# AWS EC2 setup (minimal)

- Create Ubuntu EC2, SSH from your IP only.
- Install: python3, python3-venv, git.
- Clone repo, create venv, install requirements.
- Run bot via systemd: see `infra/systemd/quant-bot.service`.
